# Medicine Reminder App

Flutter source for the Med Reminder application.

Run from this directory:

```bash
flutter pub get
flutter run
```

See the repository root `README.md` for the full feature list, Firebase setup notes, and notification guidance.
